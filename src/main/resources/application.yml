spring:
  profiles:
    active:
      - local
    group:
      local:
        - local-db
        - local-server
      test:
        - prod-db
        - local-server
      prod:
        - prod-db
        - prod-server
---
spring:
  config:
    activate:
      on-profile: local-db
  datasource:
    url: jdbc:mysql://localhost:3306/pictain?serverTimezone=UTC&characterEncoding=UTF-8
    username: pictain
    password: pictain123456789
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
---
spring:
  config:
    activate:
      on-profile: local-server
server:
  port: 8080
---
